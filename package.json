{
  "name": "nodejs-build-deploy-pipeline",
  "version": "1.0.0",
  "description": "Node.js app with a Jenkins build/test/deploy pipeline (Docker + Kubernetes)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
